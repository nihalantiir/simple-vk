# external/

Vendored dependencies fetched by CMake (FetchContent), not committed to the
repository. Dear ImGui is pinned to an exact commit in `CMakeLists.txt`;
bump that commit deliberately, never track a floating branch. Do not
hand-edit anything under here. It is regenerated on every clean configure.
